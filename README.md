# Каталог фильмов

Веб-приложение для просмотра каталога фильмов с возможностью поиска и просмотра детальной информации о фильмах.

## 🚀 Запуск

В отдельном терминале:

```bash
npx json-server --watch db.json --port 3000
```

Приложение использует json-server для имитации API. Все данные хранятся в `db.json`. API будет доступен по адресу: `http://localhost:3000/films`

```bash
ng serve
```

Приложение будет доступно по адресу: `http://localhost:4200`
